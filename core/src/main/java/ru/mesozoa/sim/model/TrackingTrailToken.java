package ru.mesozoa.sim.model;

/**
 * Один жетон следа М-травоядного.
 *
 * Жетон хранит клетку, откуда зверь ушёл, клетку, куда след ведёт, и направление
 * стрелки. В настолке это физический жетон на карте; в симуляции он нужен AI,
 * логам и отрисовке, чтобы выслеживание было видно как отдельное состояние партии.
 *
 * @param from клетка, где охотник потерял динозавра после попытки
 * @param to клетка, куда ушёл динозавр
 * @param direction направление следа от старой клетки к новой
 * @param stepNumber номер следа в текущей цепочке выслеживания
 */
public record TrackingTrailToken(Point from, Point to, Direction direction, int stepNumber) {
}
